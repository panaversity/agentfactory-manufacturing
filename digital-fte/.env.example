# The only key this base needs. Copy this file to .env and paste your key in.
# Never commit the filled-in .env (.gitignore already blocks it).
# The Quick Win assumes an OpenAI key. Other OpenAI-compatible providers (DeepSeek,
# OpenRouter) also need OPENAI_BASE_URL set, which the course's later parts add.
OPENAI_API_KEY=
