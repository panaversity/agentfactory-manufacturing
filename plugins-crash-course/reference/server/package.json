{
  "name": "agent-factory-mcp-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Remote (Streamable HTTP) MCP server for the agent-factory plugin to point at. Deploy this; the plugin wires it by URL.",
  "scripts": { "start": "node index.js", "test": "node test_server.mjs" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^4.21.0",
    "zod": "^3.23.0"
  }
}
