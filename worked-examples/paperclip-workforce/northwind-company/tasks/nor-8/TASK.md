---
name: "Build landing-page.html from the approved hero copy"
assignee: "cmo"
project: "northwind-site"
---

Turn the landing-page copy you already wrote (in NOR-2) into a real, self-contained landing-page.html file in this project's workspace folder.

DELIVERABLE: a file named exactly `landing-page.html` written to the root of your working directory (the project workspace). The FILE is the deliverable. Do NOT just paste HTML into a comment — write the actual file to disk, then confirm it exists.

Use this exact copy (the primary variant you drafted):
- Hero headline: AI you can use this week
- Subheadline: One practical read each week on the AI tools, workflows, and decisions that help operators and builders move faster without the hype.
- Call to action (button): Get the weekly brief
- Signup helper text: No spam. One email every Friday. Unsubscribe anytime.

Requirements for the file:
- A single standalone HTML file with inline CSS (no external dependencies, no build step). It must open correctly by double-clicking it in a browser.
- A hero section: the headline as the main <h1>, the subheadline below it, an email input + a button with the CTA label, and the helper text under the form.
- Clean, modern, readable styling. Mobile-friendly. This is a real landing page for the Northwind wee
